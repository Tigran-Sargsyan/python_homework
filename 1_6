def int_sum1(a,b):
    summ = 0
    if a<b:
        for i in range(a,b+1):
            summ+=i
    else:
        for i in range(b,a+1):
            summ+=i
    return summ

#2 Method
def int_sum2(a,b):
    if a>b:
        return sum(range(b,a+1))
    return sum(range(a,b+1))    
